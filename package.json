{
  "name": "desafio-ppt",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.8.9",
    "parcel": "^2.10.1"
  },
  "scripts": {
    "dev": "parcel ./src/index.html --no-cache",
    "build": "parcel build ./src/index.html --no-cache --dist-dir build"
  }
}
